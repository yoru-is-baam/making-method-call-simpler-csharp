namespace preserve_whole_object.afterpreserve
{
	internal class Restaurant
	{
		public bool IsWithinDeliveryRange(Address address)
		{
			// logic to determine if the address is within the delivery range of the restaurant
			return true;
		}
	}
}
